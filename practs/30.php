<?php
$servername = "ваш_хост";
$username = "ваше_имя_пользователя";
$password = "ваш_пароль";
$dbname = "Фирма";

// Создаем соединение
$conn = new mysqli($servername, $username, $password, $dbname);

// Проверяем соединение
if ($conn->connect_error) {
    die("Ошибка подключения к базе данных: " . $conn->connect_error);
}

// Выбираем данные из таблицы
$sql = "SELECT * FROM Сотрудники";
$result = $conn->query($sql);

// Выводим данные в цикле
if ($result->num_rows > 0) {
    while($row = $result->fetch_assoc()) {
        echo "ID: " . $row["id"]. " - Фамилия: " . $row["Фамилия"]. " Имя: " . $row["Имя"]. " Должность: " . $row["Должность"]. "<br>";
    }
} else {
    echo "0 результатов";
}

// Закрываем соединение
$conn->close();
?>

INSERT INTO Сотрудники (фамилия, имя, должность) VALUES
('Иванов', 'Иван', 'Менеджер'),
('Петров', 'Петр', 'Разработчик'),
('Сидорова', 'Мария', 'Дизайнер'),
('Козлов', 'Алексей', 'Тестировщик'),
('Антонов', 'Дмитрий', 'Аналитик');